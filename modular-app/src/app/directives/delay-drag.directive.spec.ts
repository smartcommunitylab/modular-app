import { DelayDragDirective } from './delay-drag.directive';

describe('DelayDragDirective', () => {
  it('should create an instance', () => {
    const directive = new DelayDragDirective();
    expect(directive).toBeTruthy();
  });
});
